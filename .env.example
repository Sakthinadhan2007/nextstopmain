HOST=0.0.0.0
PORT=8787
DATABASE_URL=postgres://postgres:postgres@localhost:5432/next_stop
# Keep empty in local dev to use Vite proxy (/api -> backend).
VITE_API_URL=
# Twilio (SafeStop SOS SMS — register at twilio.com)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=+1xxxxxxxxxx
