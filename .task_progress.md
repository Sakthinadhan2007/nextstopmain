# Task Progress

## Analysis (completed)
- [x] Identify Android-specific vs shared files
- [x] Confirm Android is out of sync with web (missing map, different header, sign-in wall)

## Changes Made

### Build Configuration
- [x] Add Google Maps Compose dependency to `android/app/build.gradle.kts`
- [x] Add map API key to `android/app/src/main/res/values/strings.xml`
- [x] Add API key meta-data to `android/app/src/main/AndroidManifest.xml`

### MainNavigation.kt — Synced with web `client/src/App.tsx`
- [x] Remove sign-in wall from HomeScreen (was blocking mode cards until sign-in)
- [x] Add Google Maps `ERANGMap` composable to ModeScreen (was completely missing)
- [x] Sync TopBar to match web `.top-nav` (logo, nav links, sign in/out)
- [x] Sync StatusBar to match web `.system-bar` (status text + tracking dot)
- [x] Sync mode cards to match web `.home-card` (route/stop counts, "Explore" button)
- [x] Sync RoutesScreen to match web `.routes-page` (search bar, grouped routes, preview stops)
- [x] Add Cancel button to SignInScreen (modal-like behavior matching web)
- [x] Remove unused imports (CameraUpdateFactory, LatLngBounds)

### Theme — Synced with web CSS `:root`
- [x] Update `theme/Color.kt` to use ERANGU color tokens (was default Material 3 Purple/Pink)
- [x] Update `theme/Theme.kt` to use ERANGU dark color scheme (was default Material 3)

### Cleanup
- [x] Delete unused stub files: `Navigation.kt`, `NavigationKeys.kt`, `DataRepository.kt`, `MainScreen.kt`, `MainScreenViewModel.kt`

### SafeStop Module — Implementation
- [x] Verify `shared/schema.ts` already has safety columns on `users` + `safetyEvents` table
- [x] Verify `shared/routes.ts` already has safety Zod schemas + types
- [x] Verify `server/safety.ts` already has storage helpers + Twilio SMS sender
- [x] Verify `server/app.ts` already has 3 safety routes appended before 404
- [x] Verify `client/src/components/SafeStopModal.tsx` already exists
- [x] Verify `client/src/lib/safeStop.ts` already has early-exit detection engine
- [x] Verify `client/src/lib/api.ts` already has safety API functions
- [x] Add safety section (traveler category + contacts) to sign-in form in `client/src/App.tsx`
- [x] Add `<SafeStopModal>` render in `client/src/App.tsx`
- [x] Add SafeStop CSS styles to `client/src/styles.css`
- [x] Add Twilio env vars to `.env.example`

### Git & Render Deployment
- [x] Commit SafeStop module changes to git
- [x] Push to origin/main (triggers Render auto-deploy)
- [x] Move top-nav 8px upward to fix scroll overlap
- [x] Fix render.yaml: added `buildCommand: npm run build` and changed `staticPublishPath` from `./public` to `./dist` so static front-end actually builds
- [x] Fix navbar gap: set `top: 0` on `.top-nav` to flush flush to viewport top
- [ ] Add Twilio env vars to Render dashboard: `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_FROM_NUMBER`
- [ ] Run `npx drizzle-kit push` against production DB to create `safety_events` table and add nullable columns to `users`

### Verification
- [ ] Verify changes compile correctly
